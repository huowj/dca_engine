# dca_engine

## Round 4 Validation Scope

当前结果仅属于 **Round 4 interface alignment / mock scenario acceptance**。

本轮输出用于验证：

- interface replay 是否稳定
- mock scenario 是否可重复
- metrics 是否覆盖 seq_gap / loss_detected / PPS 状态
- corrected_time_us 是否满足 mock replay 下的单调性要求

本轮结果 **不代表 Authority Mode PASS**，也 **不代表真实硬件证据**。

Current results are for **Round 4 interface alignment / mock scenario acceptance only**.  
They are **not Authority Mode PASS** and are **not real hardware evidence**.
